# Copy this file to android/keystore.properties and set real values.
# This file is intentionally safe to commit; the real keystore.properties must stay private.

# Path is resolved relative to android/app/build.gradle.kts
# Example for current repo layout:
storeFile=../../android.keystore
storePassword=CHANGE_ME
keyAlias=android
keyPassword=CHANGE_ME
